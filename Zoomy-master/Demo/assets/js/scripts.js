!function ($) {
  $(function(){
	
	
	$('.zoom').zoomy({
  innerZoom: true,
  lensHeight: 60,
  lensWidth: 100
});
	 
	
    // make code pretty
    window.prettyPrint && prettyPrint()
})
}(window.jQuery)
